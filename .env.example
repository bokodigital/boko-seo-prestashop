# Long random string used to encrypt the session cookie. openssl rand -hex 32
SESSION_SECRET=replace_with_a_long_random_string
# Store URL, username and Webservice API key are entered on the Connect screen.
# Meta generation is FREE rule-based logic - no AI key required.
